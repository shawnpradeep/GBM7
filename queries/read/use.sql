/*
To execute each block statement, place your cursor in the block and use CTRL + Enter.
The USE keyword will select the database we wish to use. (look at schemas panel after execution)
*/
-- @block
USE sql_store;

-- @block
USE SQL_inventory;

-- This is a comment, so the SQL engine is not going to execute this.